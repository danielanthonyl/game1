{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run C++",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/app.exe", // Path to the compiled executable
      "args": [],
      "stopAtEntry": false,
      "cwd": "${fileDirname}/bin",
      "environment": [],
      "preLaunchTask": "Build",
      "console": "integratedTerminal",
    }
  ]
}